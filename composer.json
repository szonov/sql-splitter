{
    "name": "szonov/sql-splitter",
    "description": "Library for parsing strings with multiple sql queries and split it to single queries",
    "license": "MIT",
    "authors": [
        {
            "name": "Sergey Zonov",
            "email": "sergey@zonov.com"
        }
    ],
    "minimum-stability": "dev",
    "require": {
        "php": ">=8.1.0",
        "szonov/text-source": "^1.0",
        "szonov/text-parser": "^2.0"
    },
    "autoload": {
        "psr-4" : {
            "SZonov\\SQL\\Splitter\\"     : "src/"
        }
    }
}
